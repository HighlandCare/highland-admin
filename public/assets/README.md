# Highland Admin Assets

Place Figma-exported assets here:

- `icons/` – UI icons, nav icons
- `images/` – logos, illustrations, hero images

Reference in code as `/assets/icons/...` or `/assets/images/...`.
